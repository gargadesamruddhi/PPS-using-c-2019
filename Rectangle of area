#include<stdio.h>
int main()
{
float length, width,area;
printf("Enter the value oflengh");
scanf("%f",& length);
printf("Enter the value of width");
scanf("%f",&width);
area=length*width;
printf("area of rectangle is:%f",area);
return 0;
}
